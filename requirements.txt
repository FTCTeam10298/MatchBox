# Core MatchBox dependencies
obs-websocket-py==1.0
websockets==10.4

# mDNS/Zeroconf support
zeroconf>=0.131.0

# GUI theme (optional, recommended for Linux)
sv-ttk>=2.0.0

# Packaging
pyinstaller>=6.0.0
zstandard>=0.22.0  # For extracting MSYS2 packages during build (Windows rsync)